# Required for /api/run
OPENAI_API_KEY=

# Auto-provisioned by `vercel env pull` (required for Vercel Sandbox)
VERCEL_OIDC_TOKEN=

# Optional: default model when the request body omits "model"
OPENAI_DEFAULT_MODEL=gpt-4.1-mini
